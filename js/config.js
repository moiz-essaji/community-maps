/**
 * Project configuration.
 *
 * Contributors: after forking, point GITHUB_REPO at your own fork so the
 * "Add a masjid" / "Report" buttons open issues on the right repository.
 */
window.BMAPS_CONFIG = {
  // "owner/repo" on GitHub. All contribution links are built from this.
  GITHUB_REPO: "moizessaji/bmaps",

  // Initial map view (roughly centres India).
  MAP_CENTER: [22.5, 78.9],
  MAP_ZOOM: 5,
};
